import { multiply } from '@src/math/math';

describe('This is a add.spec.ts test inside the  __tests__ folder', () => {
  it('this should fail until refactor', () => {
    expect(multiply(2, 2)).toBe(4);
  });
});
